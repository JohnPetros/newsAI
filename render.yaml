services:
  - type: web
    name: newsai
    runtime: python
    buildCommand: pip install uv && uv sync --frozen --no-dev
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir src
